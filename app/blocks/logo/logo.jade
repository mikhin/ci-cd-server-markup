mixin logo(props)
	- props = typeof props != 'undefined' ? props : {}

	- var attrs = props.attrs || {}

	if attrs.href
		a.logo(
			class='logo_type_link'
		)&attributes(attrs) School CI server
	else
		span.logo School CI server
